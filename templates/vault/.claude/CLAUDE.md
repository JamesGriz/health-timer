# Vault session instructions

This file is read on every session start. See the vault root `CLAUDE.md` for the full spec — it's the canonical source.

Key reminders:

- **Orient first**: read `_meta/_master-index.md`.
- **Preserve frontmatter** on edits; update `updated:`.
- **Append to index** when you create a new note.
- **Link by ID**: `[[YYYYMMDDHHMM-slug]]`.
- **One idea per zettel.**
- **Never delete `Daily/**` or `Archive/**`** without an explicit filename.
